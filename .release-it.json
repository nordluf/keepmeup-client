{
  "git": {
    "requireCleanWorkingDir": false,
    "commit": false,
    "tag": true,
    "tagName": "v${version}"
  },
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": false
    }
  },
  "hooks": {
    "before:init": "npm pkg set version=$(echo \"$(npm pkg get version | tr -d \\\"; git tag --list 'v*' | cut -c2-)\" | sort -V | tail -1)"
  }
}